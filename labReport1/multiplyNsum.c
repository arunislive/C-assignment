// Write a program to multiply two numbers (10 and 8) and display its sum.
#include <stdio.h>
int main()
{
    int a = 10, b = 8;
    printf("a = %d, b = %d,\n a*b = %d,\n a+b = %d", a, b, a * b, a + b);
    return 0;
}